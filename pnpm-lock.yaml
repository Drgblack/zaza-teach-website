lockfileVersion: '6.0'

settings:
  autoInstallPeers: true
  excludeLinksFromLockfile: false

dependencies:
  '@emotion/is-prop-valid':
    specifier: latest
    version: latest
  '@hookform/resolvers':
    specifier: ^3.9.1
    version: 3.9.1
  # Additional dependencies will be resolved by pnpm install

packages:
  # Package registry entries will be populated by pnpm install